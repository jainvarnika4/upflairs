client-side prog lang. - seen to the user - help in descision making or styling etc.
can not change database

used for Dynamic behaviour of website
// - comments

Fundamentals--
1. writing a simple JS on Web breowser - Dev Tools _ console
2. Data types
   numbers (45, 8.4, -34, -34.2)
   string  (" ", ' ', ''+''=concatenation; ''.length; ''.indexOf(''); ''[1])
   boolean  (true, false )
   array  (['' , 45] -- heterogeneous elements(diff data types); [].length)
   objects (collection or group of key-value pairs-- {name:'varnika', age:21} )

3. operators  ( =, +=, ++)
4. variables  (var a=10 -- only hav eto use 'var' it itself decides the data type)
5. Built-in functions
6. using javascript in HTML
   a. Inline
   b. internal
   c. external
7. control statements
8. looping statements
   while
   for
   for in 
9. user defined functions

objects--- console.write func, window.alert func
          document 
          form data
          form elements



#in Console --> alert()   console.log()  prompt() 
# var m = {name:'varnika', age:21}
# undefined
# m
# {name: 'varnika', age: 21}
# m[name]
# undefined
# m['name']
# 'varnika'
# m['name'][0]
# 'v'


10. Document object Modelling
         document.write()
         document.URL - gives url of page
         document.head - gives content of head tag
         document.body - content of body tag
         document.links - provides links presentin code
         {document.links[0]-- provides the link}

         i. Selecting the HTML tags
             document.getElementsByTagName ( eg --var heading = document.getElementsByTagName('h1'))
             document.getElementById
             document.getElementsByClassName (eg.--var lists = document.getElementsByClassName('list') 
                                                 --for(var i=0;i<lists.length;i++){lists[i].style.fontSize='50px'})
             document.querySelector -- selects only 1 (--id->'#' --class->'.')
             document.querySelectorAll -- selects all of that type

         ii. Interaction with the Document
             domObject.textContent - changes only text 
             domObject.innerHTML - 
             domObject.style -- (eg.-- heading[0].style.color = 'red', .style.paddingBottom = '20px')
             domObject.getAttribute -- (eg.-- variable--anc.getAttribute('href'))
             domObject.setAttribute -- (eg. --anc.setAttribute('href','https://google.com'))
             domObject......
             domObject.appendChild --(eg.--list1.appendChild(list2.childNodes[1]))--adds item1 of list2 in list1
             domObject.childNodes[n] 

         iii. Events 
               domObject.addEventListener(event,listener)
                  
         